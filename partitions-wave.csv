# Waveshare ESP32-S3-LCD-1.47B: 16 MB flash, no recovery bootloader to
# preserve, so a plain single-app layout -- with an environment log after it.
#
# envlog is a raw partition, not a filesystem: it holds a ring of 4 KB sectors
# written twelve bytes at a time, one reading a minute. 512 KB is 128 sectors
# of 340 readings, which is 43,520 readings -- thirty days and a little over.
# The size IS the retention: when the ring wraps it erases the oldest sector.
# See main/envstore.c.
#
# Name,     Type, SubType,  Offset,   Size,  Flags
nvs,        data, nvs,      0x9000,   24K,
phy_init,   data, phy,      0xf000,   4K,
factory,    app,  factory,  0x10000,  4M,
envlog,     data, 0x40,     0x410000, 512K,
